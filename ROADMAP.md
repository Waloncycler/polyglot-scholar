# Polyglot Scholar 产品优化路线图

本以此文档记录产品功能优化的规划与进度。

## 1. 交互体验升级：文件直接导入 (File Import)
- [x] **支持格式**：.docx, .xlsx, .txt, .md
- [x] **功能描述**：
    - 在控制面板或输入区增加文件上传组件。
    - 使用现有的 `fileParser.ts` 解析文件内容。
    - 自动填充到“原文输入框”中，保留基本的分段格式。
- [x] **状态**：已完成

## 2. 专业度提升：术语一致性 (Glossary & Terminology)
- [x] **功能描述**：
    - 新增“术语表”配置界面（UI）。
    - 允许用户添加 `原文 -> 译文` 对（例如：`Agent -> 智能体`）。
    - 在构建 Prompt 时，将术语表作为约束条件注入给 LLM。
- [x] **状态**：已完成

## 3. 深度优化：上下文感知翻译 (Context Awareness)
- [x] **功能描述**：
    - 优化翻译逻辑，不仅仅发送当前段落。
    - 将“上一段的译文”或“前序摘要”作为 Context 传递给模型。
    - 解决代词（It, They）指代不清和语境断裂的问题。
- [x] **状态**：已完成

## 4. 生产力工具：所见即所得编辑与导出 (Post-editing & Export)
- [x] **所见即所得编辑**：
    - 改造 `ParallelTranslationView` / `OutputArea`，使译文区域可直接编辑。
    - 支持用户手动修正 AI 的翻译错误。
- [x] **格式化导出**：
    - 支持将最终的（修正后的）译文导出为 Markdown 或 Text 文档。
    - 保留原文的基本结构。
- [x] **状态**：已完成
